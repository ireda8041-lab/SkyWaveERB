# 📋 سجل التغييرات - الإصدار 1.3.6

## 🗓️ تاريخ الإصدار: 11 يناير 2026

## 🎯 الهدف الرئيسي
**إصلاحات جذرية للاستقرار - تعطيل الأنظمة التي تسبب التجميد والإغلاق المفاجئ**

---

## 🛡️ الأنظمة المعطّلة (كانت تسبب التجميد)

### 1. نظام المزامنة المتقدم (`core/advanced_sync_manager.py`)
- ❌ **ConnectionChecker** - كان يفحص الاتصال كل 10 ثواني باستخدام `requests.get` الذي يتجمد
- ❌ **SyncWorker** - كان يعمل في الخلفية ويسبب تجميد
- ❌ **AdvancedSyncManagerV3** - لم يعد يشغل أي threads

### 2. الصيانة الدورية (`main.py`)
- ❌ **maintenance_worker** - كان يعمل كل ساعة ويسبب تجميد

### 3. نظام المزامنة الفورية (`core/realtime_sync.py`)
- ❌ **Change Streams** - معطّل بالفعل من الإصدار السابق

---

## ⚡ التحسينات

### 1. تقليل الـ Timers
| المكون | قبل | بعد |
|--------|-----|-----|
| تحديث المهام (TodoManager) | 1 دقيقة | 5 دقائق |
| فحص الاتصال | 10 ثواني | معطّل |
| SyncWorker | 1 دقيقة | معطّل |

### 2. تقليل الـ Threads النشطة
- **قبل:** ~8 threads نشطة (ConnectionChecker, SyncWorker, maintenance, realtime sync, etc.)
- **بعد:** ~2 threads فقط (unified sync, notification sync)

### 3. نظام المزامنة الموحد (الوحيد النشط)
- `UnifiedSyncManagerV3` هو النظام الوحيد للمزامنة الآن
- يعمل بفترات طويلة (ساعة للمزامنة الكاملة، 15 دقيقة لرفع التغييرات)
- أكثر استقراراً وأقل استهلاكاً للموارد

---

## 🔧 الملفات المعدّلة
- `core/advanced_sync_manager.py` - تعطيل ConnectionChecker و SyncWorker
- `ui/todo_manager.py` - زيادة فترة التحديث الدوري
- `ui/payments_manager.py` - إضافة SizePolicy للتجاوب التلقائي
- `main.py` - تعطيل الصيانة الدورية
- `version.py` - رقم الإصدار
- `version.json` - معلومات الإصدار

---

## 📱 تحسينات الـ Layout
- جميع الشاشات تستخدم `SizePolicy.Expanding` للتجاوب التلقائي
- الجداول تتمدد لتملأ المساحة المتاحة
- الأزرار والحقول تتجاوب مع حجم النافذة

---

## 📝 ملاحظات مهمة
- البرنامج الآن أكثر استقراراً بشكل ملحوظ
- المزامنة لا تزال تعمل لكن بشكل أبطأ وأكثر أماناً
- يمكن عمل مزامنة يدوية في أي وقت من القائمة
- البيانات المحلية (SQLite) تعمل بشكل فوري دائماً
- إذا استمرت المشاكل، يرجى مراجعة ملف الـ log في:
  `%LOCALAPPDATA%\SkyWaveERP\logs\skywave_erp.log`
