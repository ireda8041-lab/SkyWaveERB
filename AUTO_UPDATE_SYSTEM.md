# 🔄 نظام التحديث التلقائي - Sky Wave ERP

## الميزة

نظام تحديث تلقائي يتحقق من وجود إصدارات جديدة ويقوم بتحميلها وتثبيتها.

## كيف يعمل

### 1. التحقق التلقائي عند بدء البرنامج
```
فتح البرنامج
    ↓
التحقق من version.json على GitHub
    ↓
مقارنة الإصدار الحالي (1.0.3) مع الأحدث
    ↓
إذا يوجد تحديث → إشعار المستخدم
```

### 2. التحديث اليدوي
```python
from auto_updater import check_and_update

# التحقق والتحديث
check_and_update()
```

## الملفات

### 1. `auto_updater.py` - نظام التحديث
```python
# الوظائف الرئيسية
check_for_updates()      # التحقق من التحديثات
download_update()         # تحميل التحديث
apply_update()            # تطبيق التحديث
check_and_update()        # كل شيء في دالة واحدة
```

### 2. `version.json` - معلومات الإصدار
```json
{
    "version": "1.0.3",
    "release_date": "2025-12-03",
    "download_url": "https://github.com/.../v1.0.3/SkyWaveERP-Setup.exe",
    "changelog": [
        "⚡ المزامنة التلقائية",
        "✅ الترتيب بالجداول",
        "..."
    ]
}
```

### 3. `updater.exe` - برنامج التحديث
- يقوم بفك ضغط الملفات الجديدة
- يستبدل الملفات القديمة
- يعيد تشغيل البرنامج

## الاستخدام

### من داخل البرنامج (تلقائي)
```python
# في main.py - يتم تلقائياً
from auto_updater import check_for_updates
has_update, latest_version, download_url, changelog = check_for_updates()
```

### من زر في الواجهة
```python
from auto_updater import check_and_update

def on_update_button_clicked():
    check_and_update(auto_download=False)
```

### من سطر الأوامر
```bash
python auto_updater.py
```

## الرسائل

### عند وجود تحديث
```
🔍 جاري التحقق من التحديثات...
✅ يوجد تحديث جديد: v1.0.4

================================================================================
🎉 تحديث جديد متوفر!
================================================================================
📦 الإصدار الحالي: v1.0.3
🆕 الإصدار الجديد: v1.0.4

📋 التغييرات:
  1. ⚡ تحسين الأداء
  2. 🐛 إصلاح الأخطاء
  3. ✨ ميزات جديدة
================================================================================

❓ هل تريد تحميل التحديث الآن؟ (yes/no):
```

### أثناء التحميل
```
📥 جاري تحميل التحديث من: https://...
📊 التقدم: 45.3%
✅ تم تحميل التحديث: update_temp.zip
```

### عند التطبيق
```
🔄 جاري تطبيق التحديث...
✅ تم تشغيل المحدث
🔄 سيتم إغلاق البرنامج وتطبيق التحديث...
```

## المتطلبات

### Python Packages
```bash
pip install requests packaging
```

### الملفات المطلوبة
- ✅ `auto_updater.py` - نظام التحديث
- ✅ `updater.exe` - برنامج التحديث
- ✅ `version.json` - على GitHub

## الإعداد

### 1. رفع version.json على GitHub
```bash
# في مستودع GitHub
https://github.com/imhzm/SkyWaveERB/main/version.json
```

### 2. تحديث الرابط في auto_updater.py
```python
VERSION_URL = "https://raw.githubusercontent.com/imhzm/SkyWaveERB/main/version.json"
```

### 3. رفع الإصدار الجديد
```bash
# إنشاء Release على GitHub
# رفع SkyWaveERP-Setup.exe
# تحديث version.json
```

## سير العمل

### عند إصدار نسخة جديدة

1. **تحديث رقم الإصدار**
   ```python
   # في version.py
   CURRENT_VERSION = "1.0.4"
   
   # في auto_updater.py
   CURRENT_VERSION = "1.0.4"
   ```

2. **بناء EXE**
   ```bash
   python build_exe.py
   ```

3. **إنشاء Release على GitHub**
   - اذهب إلى Releases
   - اضغط "New Release"
   - Tag: v1.0.4
   - رفع SkyWaveERP-Setup.exe

4. **تحديث version.json**
   ```json
   {
       "version": "1.0.4",
       "release_date": "2025-12-04",
       "download_url": "https://github.com/.../v1.0.4/SkyWaveERP-Setup.exe",
       "changelog": [...]
   }
   ```

5. **رفع version.json على GitHub**
   ```bash
   git add version.json
   git commit -m "Update to v1.0.4"
   git push
   ```

## الأمان

### التحقق من التوقيع (اختياري)
```python
import hashlib

def verify_download(file_path, expected_hash):
    with open(file_path, 'rb') as f:
        file_hash = hashlib.sha256(f.read()).hexdigest()
    return file_hash == expected_hash
```

### استخدام HTTPS
- ✅ الرابط يستخدم HTTPS
- ✅ التحقق من الشهادة

## حل المشاكل

### المشكلة 1: فشل الاتصال
```
⚠️ فشل الاتصال بالسيرفر
```
**الحل:** تحقق من الإنترنت والرابط

### المشكلة 2: فشل التحميل
```
❌ فشل تحميل التحديث
```
**الحل:** تحقق من مساحة القرص والصلاحيات

### المشكلة 3: updater.exe مفقود
```
❌ لم يتم العثور على updater.exe
```
**الحل:** تأكد من وجود updater.exe في نفس المجلد

## الاختبار

### اختبار محلي
```python
# تعديل VERSION_URL مؤقتاً
VERSION_URL = "file:///D:/blogs/SkyWaveERB/version.json"

# تشغيل
python auto_updater.py
```

### اختبار على GitHub
```python
# استخدام الرابط الحقيقي
VERSION_URL = "https://raw.githubusercontent.com/imhzm/SkyWaveERB/main/version.json"

# تشغيل
python auto_updater.py
```

## الإحصائيات

```python
from auto_updater import get_current_version, get_latest_version_info

# الإصدار الحالي
current = get_current_version()
print(f"الإصدار الحالي: {current}")

# معلومات آخر إصدار
info = get_latest_version_info()
print(f"آخر إصدار: {info['version']}")
print(f"تاريخ الإصدار: {info['release_date']}")
```

## الخلاصة

### ما تم إضافته
- ✅ نظام تحديث تلقائي كامل
- ✅ التحقق عند بدء البرنامج
- ✅ تحميل وتطبيق التحديثات
- ✅ عرض التغييرات (Changelog)
- ✅ معالجة الأخطاء

### النتيجة
- 🎯 **تحديثات سهلة** - نقرة واحدة
- 🎯 **تلقائي** - يتحقق عند البدء
- 🎯 **آمن** - HTTPS + معالجة الأخطاء
- 🎯 **احترافي** - عرض التقدم والتغييرات

---

**تاريخ الإضافة:** 2025-12-03  
**الحالة:** ✅ جاهز للاستخدام  
**النتيجة:** نظام تحديث احترافي 🚀
