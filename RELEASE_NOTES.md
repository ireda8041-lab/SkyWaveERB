# 🚀 Sky Wave ERP - Release Notes

## Version 1.0.3 (2025-12-06)

### ✅ تم التحديث بنجاح!

تم إصدار النسخة 1.0.3 من Sky Wave ERP مع إصلاحات مهمة وتحسينات في الأداء.

### 📦 ما تم إنجازه:

1. ✅ تحديث رقم الإصدار في `version.py` إلى `1.0.3`
2. ✅ بناء التطبيق باستخدام PyInstaller
3. ✅ إنشاء ملف Setup باستخدام Inno Setup
4. ✅ رفع التغييرات على GitHub
5. ✅ نشر Release على GitHub مع ملف التثبيت

### 🔧 التحسينات والإصلاحات:

- ✅ إصلاح مشكلة تغيير أرقام الفواتير
- ✅ إصلاح مشكلة تكرار الخدمات
- ✅ إصلاح مشكلة زر تسجيل الخروج
- ✅ تحسين ارتفاع صفوف الجداول
- ✅ تحسين أداء المزامنة

### 📥 التحميل:

الملف متاح على GitHub:
- **الرابط:** https://github.com/ireda8041-lab/SkyWaveERB/releases/download/1.0.3/SkyWave-Setup-1.0.3.exe
- **الحجم:** ~303 MB

### 🎯 كيف يعمل نظام التحديث:

1. المستخدم يفتح البرنامج (الإصدار القديم مثلاً 1.0.2)
2. البرنامج يتحقق من `version.json` على GitHub
3. يجد إصدار جديد (1.0.3)
4. يعرض رسالة "يوجد تحديث جديد"
5. المستخدم ينزل ويثبت التحديث
6. بعد التثبيت، البرنامج يقرأ الإصدار الجديد (1.0.3)
7. لا توجد رسالة تحديث لأن الإصدار متطابق ✅

### 📝 ملاحظات مهمة:

- تأكد من إغلاق البرنامج قبل تثبيت التحديث
- التحديث يحتفظ بجميع البيانات والإعدادات
- يُنصح بعمل نسخة احتياطية من قاعدة البيانات قبل التحديث

### 🔄 للإصدارات القادمة:

عند إصدار نسخة جديدة (مثلاً 1.0.4):
1. حدّث `CURRENT_VERSION` في `version.py`
2. نفذ: `python -m PyInstaller SkyWaveERP.spec --noconfirm`
3. نفذ: `& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "SkyWaveERP_Setup.iss"`
4. نفذ: `git add . && git commit -m "Update to version 1.0.4" && git push`
5. نفذ: `gh release create 1.0.4 "installer_output\SkyWave-Setup-1.0.4.exe" --title "Sky Wave ERP v1.0.4" --notes "Release notes here"`

---

**تم بنجاح! ✨**
