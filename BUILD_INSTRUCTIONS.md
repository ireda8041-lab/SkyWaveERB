# 🚀 تعليمات بناء SkyWave ERP v1.0.5

## ✅ الفحص الشامل
تم إنشاء ملف `check_project.py` للتأكد من وجود كل الملفات الضرورية.

```bash
python check_project.py
```

## 📦 بناء البرنامج

### الطريقة 1: استخدام السكريبت الجاهز (موصى به)
```bash
python build_exe.py
```

### الطريقة 2: أمر PyInstaller المباشر
```bash
pyinstaller --noconfirm --onedir --windowed --name "SkyWaveERP" --icon "icon.ico" --add-data "ui;ui" --add-data "services;services" --add-data "core;core" --add-data "assets;assets" --add-data "skywave_settings.json;." --add-data "version.json;." --add-data "version.py;." --add-data "updater.py;." --add-data "update_settings.json;." --add-data "icon.ico;." --add-data "logo.png;." --add-data "site logo.png;." --add-binary "updater.exe;." --hidden-import "pymongo" --hidden-import "PyQt6" --hidden-import "PyQt6.QtCore" --hidden-import "PyQt6.QtGui" --hidden-import "PyQt6.QtWidgets" --hidden-import "PyQt6.QtWebEngineWidgets" --hidden-import "reportlab" --hidden-import "PIL" --hidden-import "openpyxl" --hidden-import "google.generativeai" --hidden-import "requests" --collect-all "reportlab" main.py
```

## 📁 الملفات المضمنة في البناء

### المجلدات الأساسية:
- ✅ `ui/` - كل واجهات المستخدم
- ✅ `services/` - كل الخدمات (محاسبة، فواتير، مسح ذكي، تحديث تلقائي)
- ✅ `core/` - النواة (قاعدة البيانات، المصادقة، المزامنة)
- ✅ `assets/` - الصور والخطوط والأيقونات

### الملفات المهمة:
- ✅ `main.py` - الملف الرئيسي
- ✅ `version.py` - الإصدار 1.0.5
- ✅ `version.json` - معلومات الإصدار
- ✅ `updater.py` - برنامج التحديث
- ✅ `updater.exe` - برنامج التحديث المجمع
- ✅ `update_settings.json` - إعدادات التحديث
- ✅ `skywave_settings.json` - إعدادات البرنامج
- ✅ `icon.ico` - أيقونة البرنامج
- ✅ `logo.png` - شعار البرنامج
- ✅ `site logo.png` - شعار الموقع
- ✅ `skywave_local.db` - قاعدة البيانات المحلية

### الخطوط:
- ✅ `assets/font/Cairo-VariableFont_slnt,wght.ttf` - الخط العربي

### المكتبات المخفية:
- ✅ PyQt6 (كل المكونات)
- ✅ PyMongo (قاعدة البيانات)
- ✅ ReportLab (PDF)
- ✅ PIL/Pillow (الصور)
- ✅ OpenPyXL (Excel)
- ✅ Google Generative AI (المسح الذكي)
- ✅ Requests (التحديثات)

## 📂 مخرجات البناء

بعد البناء، ستجد:
```
dist/
└── SkyWaveERP/
    ├── SkyWaveERP.exe          ← الملف التنفيذي الرئيسي
    ├── _internal/              ← المكتبات والملفات الداخلية
    ├── ui/                     ← واجهات المستخدم
    ├── services/               ← الخدمات
    ├── core/                   ← النواة
    ├── assets/                 ← الموارد
    ├── updater.py              ← برنامج التحديث
    ├── updater.exe             ← برنامج التحديث المجمع
    ├── version.json            ← معلومات الإصدار
    ├── icon.ico                ← الأيقونة
    ├── logo.png                ← الشعار
    └── ... (ملفات أخرى)
```

## ⚠️ ملاحظات مهمة

1. **لا تنسخ ملف EXE لوحده!** يجب نسخ المجلد `SkyWaveERP` كاملاً
2. **المجلد `_internal`** يحتوي على كل المكتبات الضرورية
3. **ملف `updater.exe`** ضروري لنظام التحديث التلقائي
4. **قاعدة البيانات** `skywave_local.db` مضمنة في البناء

## 🎯 التوزيع

### للتوزيع على المستخدمين:
1. انسخ مجلد `dist/SkyWaveERP` كاملاً
2. اضغطه في ملف ZIP
3. أو استخدم Inno Setup لعمل installer احترافي

### لعمل Installer باستخدام Inno Setup:
```bash
# استخدم ملف SkyWaveERP_Setup.iss الموجود
iscc SkyWaveERP_Setup.iss
```

## 🔄 نظام التحديث التلقائي

البرنامج يحتوي على نظام تحديث تلقائي:
- يتحقق من التحديثات كل 24 ساعة
- يتحقق عند بدء التشغيل
- يستخدم `updater.exe` لتطبيق التحديثات
- رابط التحديث: https://raw.githubusercontent.com/ireda8041-lab/SkyWaveERB/main/version.json

## 📝 الإصدار الحالي

- **الإصدار:** 1.0.5
- **التاريخ:** 2025-12-06
- **التحسينات:**
  - إصلاح مشكلة تغيير أرقام الفواتير
  - إصلاح مشكلة الخدمات المكررة
  - إصلاح مشكلة زر تسجيل الخروج
  - تحسين ارتفاع صفوف الجداول
  - تحسين أداء المزامنة

## 🆘 حل المشاكل

### المشكلة: "ملف مفقود" عند التشغيل
**الحل:** تأكد من نسخ المجلد كاملاً وليس ملف EXE فقط

### المشكلة: "خطأ في تحميل الخط"
**الحل:** تأكد من وجود مجلد `assets/font/` في البناء

### المشكلة: "خطأ في قاعدة البيانات"
**الحل:** تأكد من وجود ملف `skywave_local.db` أو سيتم إنشاؤه تلقائياً

### المشكلة: "التحديث التلقائي لا يعمل"
**الحل:** تأكد من وجود `updater.exe` و `update_settings.json`

## ✅ قائمة التحقق النهائية

- [ ] تم تشغيل `check_project.py` بنجاح
- [ ] تم تحديث الإصدار في `version.py` و `version.json`
- [ ] تم بناء البرنامج باستخدام `build_exe.py`
- [ ] تم اختبار البرنامج المبني
- [ ] تم التأكد من عمل نظام التحديث
- [ ] تم إنشاء Installer (اختياري)
- [ ] جاهز للتوزيع! 🎉
